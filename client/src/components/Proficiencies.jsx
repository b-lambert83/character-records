import React, {useState, useEffect, useContext } from 'react';

const Proficiencies = () => {
  const profObj = {

  };

  const profList = Object.keys(profObj).map(() => (
    <div></div>
  ));

  return (
    <div>
      <form id="add_proficiency" />
      Component
    </div>
  )
}

export default Proficiencies;